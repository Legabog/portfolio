import styled from 'styled-components';

export const Wrapper = styled.div`
	display: flex;
	width: calc(100% - 24px);
	justify-content: space-between;
	position: fixed;
	margin: 12px;
	z-index: 2;
`;
