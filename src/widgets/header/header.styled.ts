import styled from 'styled-components';

export const Wrapper = styled.div`
  display: flex;
  width: calc(100% - 40px);
  justify-content: space-between;
  position: fixed;
  margin: 12px 20px;
  z-index: 2;
`;
